import React from "react";
import Menu from '../Menu/Menu';

const Contacto = () => {
    return(
        <div>
          <img src="" alt="" sizes="(max-width: 120px) 100vw, 120px"/>
          <h1>CONTACTO</h1>
          <div>Para cualquier consulta podes comunicarte con la casilla de correo indicada:</div>
          <a>email@mail.com</a>
        </div>
    );
}

export default Contacto;